% !TEX program = xelatex
% NOTE: 为了更好的 Unicode 支持，请使用 XeLaTeX（或 LuaLaTeX）编译。
\documentclass[11pt,oneside]{book}

\usepackage{fontspec}
\usepackage{xeCJK}

% Use system fonts for reliability
% For Latin text, use a common system font
\setmainfont{Times New Roman}[
  BoldFont = Times New Roman Bold,
  ItalicFont = Times New Roman Italic,
  BoldItalicFont = Times New Roman Bold Italic
]

% For CJK text with fake slant for italic effect
\setCJKmainfont{Songti SC}[
  AutoFakeSlant=0.2,
  BoldFont = Songti SC Bold
]

% For monospaced CJK text
\setCJKmonofont{Heiti SC}[
  AutoFakeSlant=0.2
]

% CJK 排版改进
\XeTeXlinebreaklocale "zh"
\XeTeXlinebreakskip = 0pt plus 1pt minus 0.1pt

\xeCJKsetup{
  CJKspace = true,
  CheckSingle = true,
  PlainEquation = true
}

% 行距优化
\linespread{1.3}

\usepackage[
  paperwidth=6in,
  paperheight=9in,
  top=0.75in,
  bottom=0.75in,
  inner=1in,
  outer=0.75in,
  footskip=0.3in,
  headheight=14pt
]{geometry}
\usepackage{amsmath}
\usepackage{amssymb}  % For \square checkbox symbol
\usepackage{graphicx}
% \usepackage{tikz-cd}  % Not currently used, commented out

% 颜色定义
\usepackage{xcolor}
\definecolor{chapterblue}{RGB}{0,51,102}
\definecolor{sectiongray}{RGB}{64,64,64}

% 页眉页脚
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\nouppercase{\leftmark}}
\fancyhead[R]{\thepage}
\renewcommand{\headrulewidth}{0.4pt}

\fancypagestyle{plain}{
  \fancyhf{}
  \fancyfoot[C]{\thepage}
  \renewcommand{\headrulewidth}{0pt}
}

% PDF 优化
\usepackage{hyperref}
\hypersetup{
    unicode=true,
    pdfauthor={Li Hongmin (李鸿敏)},
    pdftitle={Research Engineering OS: 把返工压缩成规范 + 模板 + 检查清单},
    pdfsubject={Research Engineering Best Practices},
    bookmarksnumbered=true,
    bookmarksopen=true,
    bookmarksopenlevel=2,
    colorlinks=true,
    linkcolor=blue!50!black,
    urlcolor=blue!70!black
}

\setlength{\parindent}{0pt}
\setlength{\parskip}{0.6\baselineskip}

% 章节标题样式
\makeatletter
\renewcommand{\@makechapterhead}[1]{%
  \vspace*{20pt}%
  {\parindent 0pt
   \raggedright
   \color{chapterblue}
   \fontsize{24pt}{28pt}\selectfont\bfseries
   \ifnum \c@secnumdepth >\m@ne
     \thechapter\quad
   \fi
   #1\par\nobreak
   \vspace{20pt}
   \hrule height 1pt
   \vspace{20pt}
  }%
}

\renewcommand{\@makeschapterhead}[1]{%
  \vspace*{20pt}%
  {\parindent 0pt
   \raggedright
   \color{chapterblue}
   \fontsize{24pt}{28pt}\selectfont\bfseries
   #1\par\nobreak
   \vspace{20pt}
   \hrule height 1pt
   \vspace{20pt}
  }%
}

% 节标题样式
\renewcommand\section{%
  \@startsection{section}{1}{\z@}%
    {-3.5ex \@plus -1ex \@minus -.2ex}%
    {2.3ex \@plus.2ex}%
    {\normalfont\Large\bfseries\color{sectiongray}}%
}
\makeatother

\title{Research Engineering OS\\\large 把返工压缩成规范 + 模板 + 检查清单}
\author{}
\date{}

\begin{document}

\frontmatter
\maketitle
\include{chapters/copyright}
\tableofcontents

\include{chapters/00_preface}

\mainmatter

\include{chapters/01_why_flip}
\include{chapters/02_experiment_unit}
\include{chapters/03_repo_layout}
\include{chapters/04_git_proof}
\include{chapters/05_dod}
\include{chapters/06_logging}
\include{chapters/07_ai_workflow}
\include{chapters/08_multi_path}
\include{chapters/no_boom}
\include{chapters/team}

\appendix

\include{chapters/templates}
\include{chapters/ai_disasters}

\end{document}
